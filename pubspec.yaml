name: alert_notification
description: A simple alert for all the information your user should be notified of.
version: 0.0.3
homepage: https://github.com/guidotheelen/alert_notification

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
